import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cari Mobil Bekas di Bandung - Temukan Mobil Impianmu dengan Mudah",
  description:
    "Cari mobil bekas di Bandung dengan mudah melalui platform kami. Temukan berbagai pilihan mobil berkualitas, bandingkan harga, dan dapatkan penawaran terbaik untuk mobil impianmu. Mulai pencarianmu sekarang!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
